Start dmforge-3000 (3000) and open logs.
```bash
cd "D:\Dev\Workspaces\Active\DMForge" && pm2 start ecosystem.config.cjs --only dmforge-3000 && start wt.exe -d "D:\Dev\Workspaces\Active\DMForge" pwsh -NoExit -c "pm2 logs dmforge-3000"
```
